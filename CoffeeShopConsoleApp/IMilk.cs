using System;
using System.Collections.Generic;
using System.Text;

namespace CoffeeShopConsoleApp
{
    // Interface a contract. 

    //what ever class that implements this interface, here is the "things" it will have
    // u kinda have to "override" this method in the classes who implements this interface

    public interface IMilk
    {
         int MlMilk();
    }
}
